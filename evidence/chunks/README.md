# Formatted Webpack chunks

These are readability-formatted copies of the app Webpack chunks. They retain compiled Vue render functions; they are not the original Vue single-file components. The third-party `vendor.js` bundle is intentionally excluded to avoid republishing full dependency implementations.
